== Stargaze app

Suggested simulator workflow:
1. SSH into devbox;
2. Update from master or your branch on Github> "git rebase origin/master"
3. Run simulator (which builds the app before) with "make simulator"